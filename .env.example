# The app in this directory needs no keys — MSW mocks the API.
#
# The agent's keys live in its own workspace. Copy that file, not this one:
#
#   cd agent && cp .env.example .env
#
# See agent/.env.example for the full list and agent/README.md for setup.
